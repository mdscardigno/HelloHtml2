{
  "name": "HelloHTML",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "start": "vite",
    "predeploy": "npm run build",
    "deploy": "netlify deploy --prod--dir=dist"
  },
  "devDependencies": {
    "typescript": "^4.9.5",
    "vite": "^4.2.0"
  }
}
